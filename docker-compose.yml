version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: produtos-db
    restart: always
    environment:
      POSTGRES_DB: produtosdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"